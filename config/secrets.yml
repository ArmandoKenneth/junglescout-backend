# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.
# ENV['AMAZON_ACCESS_KEY']
# ENV['AMAZON_ASSOCIATE_ID']


development:
  secret_key_base: 2f279c2354f0e7019058614749d26c8664e14379963bbb866a06ea8370f2cc14b69eb2b0c0cc74daebb5a9857df23efb003a01aa7656f0bce832fe442a07943c

test:
  secret_key_base: 3a6959b51ec56610d95e6a282cc1346ab8055994985fec46eab0a10df7efdc57fae8dc3fa8dbf60ddc1b1cb2c63596172658a3c09ab93d932230a853a2d3cc8d

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
